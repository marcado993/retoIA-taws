// Layout de ancho completo para las vistas de asesor, auditoría y reglas.
export default function SinglePanelTemplate({ children }) {
  return <section className="single-panel">{children}</section>
}
